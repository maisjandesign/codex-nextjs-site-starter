/** Shared defaults. Change the motion language here, not in each section. */
export const motionConfig = {
  smooth: 1.2,
  revealDuration: 0.85,
  lineStagger: 0.075,
  revealEase: "power3.out",
  revealStart: "top 92%",
  hoverDuration: 0.4,
  hoverEase: "power2.out",
  imageScale: 1.035,
} as const;
